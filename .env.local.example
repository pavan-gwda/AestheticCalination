NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-key

# Set to "false" to bypass the login redirect (e.g. while hitting Supabase's
# email rate limit in dev). Defaults to enabled. Never disable in production.
AUTH_ENABLED=true
